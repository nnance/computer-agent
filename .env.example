# Anthropic API Key (required)
ANTHROPIC_API_KEY=your_api_key_here

# Anthropic Model Configuration (optional)

# Claude model to use for API requests
# Default: claude-sonnet-4-5
# Other options: claude-opus-4, claude-sonnet-3-7, etc.
# ANTHROPIC_MODEL=claude-sonnet-4-5

# Maximum tokens for model responses
# Default: 1024
# Adjust based on your needs and model limits
# ANTHROPIC_MAX_TOKENS=1024

# Apple Calendar Configuration (required)

# Default calendar name for calendar operations
# To find your calendar names, you can run the app and use the listCalendars tool
# Common values: your email address (e.g., name@gmail.com) or calendar name from Apple Calendar app
APPLE_CALENDAR_NAME=your_calendar_name_here

# Context File Configuration (optional)

# Path to the assistant context file that provides persistent context
# Default: ./.agent/ASSISTANT.md
# Example: ./tmp/ASSISTANT.md for legacy path
# CONTEXT_FILE_PATH=./.agent/ASSISTANT.md

# Web Search Tool Configuration (all optional)

# Maximum number of web searches per API request
# If not set, unlimited searches are allowed
# WEB_SEARCH_MAX_USES=5

# Domain filtering - only include results from these domains
# Cannot be used together with WEB_SEARCH_BLOCKED_DOMAINS
# Format: comma-separated list of domains (supports subdomains)
# WEB_SEARCH_ALLOWED_DOMAINS=example.com,trusted-source.org

# Domain filtering - exclude results from these domains
# Cannot be used together with WEB_SEARCH_ALLOWED_DOMAINS
# Format: comma-separated list of domains (supports subdomains)
# WEB_SEARCH_BLOCKED_DOMAINS=untrusted.com,spam-site.net

# User location for search context (all optional)
# Providing location helps return more relevant, localized search results
# WEB_SEARCH_USER_LOCATION_CITY=San Francisco
# WEB_SEARCH_USER_LOCATION_REGION=California
# WEB_SEARCH_USER_LOCATION_COUNTRY=US
# WEB_SEARCH_USER_LOCATION_TIMEZONE=America/Los_Angeles

# Note: Web search pricing is $10 per 1,000 searches
# Plus standard token costs for search-generated content

# Web Fetch Tool Configuration (all optional)

# Maximum number of web fetches per API request
# If not set, unlimited fetches are allowed
# WEB_FETCH_MAX_USES=10

# Domain filtering - only fetch from these domains
# Cannot be used together with WEB_FETCH_BLOCKED_DOMAINS
# Format: comma-separated list of domains (supports subdomains)
# WEB_FETCH_ALLOWED_DOMAINS=example.com,docs.site.org

# Domain filtering - exclude fetches from these domains
# Cannot be used together with WEB_FETCH_ALLOWED_DOMAINS
# Format: comma-separated list of domains (supports subdomains)
# WEB_FETCH_BLOCKED_DOMAINS=private.example.com,blocked-site.net

# Enable citations for fetched content (true/false)
# When enabled, includes source attribution for fetched content
# WEB_FETCH_ENABLE_CITATIONS=true

# Maximum content size in tokens (approximate)
# Helps control token usage for large documents
# Average webpage (10KB): ~2,500 tokens
# Large documentation (100KB): ~25,000 tokens
# Research PDF (500KB): ~125,000 tokens
# WEB_FETCH_MAX_CONTENT_TOKENS=100000

# Note: Web fetch has no additional charges beyond standard token costs
# Supports fetching text content and automatic PDF text extraction
